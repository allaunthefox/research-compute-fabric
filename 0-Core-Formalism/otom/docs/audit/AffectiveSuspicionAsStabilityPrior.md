# Affective Suspicion as Stability Prior

## Purpose

This note records a correction to the Anti-Music / AMREF line of thought.

The reaction is not distress and it is not evidence. It is an internal stability-prior warning: the concept is legible, but it violates expected structure strongly enough that the mind flags it as suspicious.

## Core Statement

```text
I understand it, but I do not like it
```

is not a proof and not a refutation. It is a useful probe signal.

In the audit stack, this becomes:

```text
affective suspicion -> stability prior activation -> require stricter receipts
```

## Interpretation

Affective suspicion means the representation is crossing a boundary between familiar lawful structure and unfamiliar inverse-structure. That makes it worth testing, but it also increases the audit burden.

Correct use:

```text
use suspicion as a routing signal for review
increase receipt requirements
keep the object silent and symbolic
run finite audits before promotion
```

Incorrect use:

```text
suspicion proves correctness
suspicion proves danger
suspicion replaces math
suspicion licenses overclaiming
```

## AMREF Handling Rule

For Anti-Music / AMREF candidates, suspicion should trigger:

```text
AUDIO_RENDER = false
finite windows only
bounded perturbation only
randomness penalty required
B2 collision penalty required
explicit difference-set and sum-set receipts required
FAMM/Inverted FAMM classification required
```

## Gate Upgrade

If a concept is understandable but aversive, classify it as:

```text
Status: SUSPECT_BUT_LEGIBLE
Gate: U_scope
Action: increase validation pressure, do not discard automatically
```

## FAMM Mapping

```text
low suspicion + valid receipts -> ordinary basin candidate
high suspicion + no receipts -> quarantine
high suspicion + coherent finite tests -> HOLD until receipts arrive
high suspicion + validated counterexample yield -> scar or new basin boundary
high suspicion + validated repeatability -> possible inverse-basin candidate
```

## Inverted FAMM Mapping

Inverted FAMM should treat this reaction as a boundary detector:

```text
where the mind says 'I understand it but I do not like it', search for:
  hidden invariants
  malformed assumptions
  overfit stability
  missing receipts
  negative-space structure
  adversarial shortcuts
```

## Boundary

Do not claim:

```text
emotional discomfort is evidence
anti-music is correct because it feels wrong
suspicion is a validator
```

Allowed claim:

```text
The suspicion response indicates that AMREF stresses a stability prior, so the object should remain silent, symbolic, finite, and audit-heavy until it earns promotion through receipts.
```

## Audit Classification

```text
Receipt: AffectiveSuspicionAsStabilityPrior
Status: SAFETY_AND_ROUTING_RULE
Gate: U_scope
Reason: coherent as a research-routing signal, but it is not evidential and must only increase validation burden rather than promote claims.
```

## Required Receipts

```text
SilentRepresentationReceipt
FiniteWindowReceipt
PerturbationBoundReceipt
RandomnessPenaltyReceipt
CollisionPenaltyReceipt
DifferenceSetReceipt
SumSetReceipt
FAMMClassificationReceipt
ValidatorReceipt
```
