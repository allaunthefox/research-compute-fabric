apiVersion: v1
kind: Service
metadata:
  name: registry-api
  namespace: services
  labels:
    app: registry-api
spec:
  type: ClusterIP
  selector:
    app: registry-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
