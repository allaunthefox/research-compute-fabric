apiVersion: v1
kind: Service
metadata:
  name: credential-server
  namespace: services
spec:
  type: ClusterIP
  selector:
    app: credential-server
  ports:
    - port: 8444
      targetPort: 8444
      name: http
