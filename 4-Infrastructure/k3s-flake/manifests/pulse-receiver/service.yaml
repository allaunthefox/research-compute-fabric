apiVersion: v1
kind: Service
metadata:
  name: pulse-receiver
  namespace: services
spec:
  type: NodePort
  selector:
    app: pulse-receiver
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30804
